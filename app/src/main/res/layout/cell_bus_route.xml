<?xml version="1.0" encoding="utf-8"?>
<layout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:card_view="http://schemas.android.com/apk/res-auto">

    <data>

        <variable
            name="busRoute"
            type="jessevivanco.com.pegcitytransit.data.rest.models.BusRoute"/>
    </data>

    <!-- TODO figure out dimens -->
    <android.support.v7.widget.CardView
        android:id="@+id/cell_container"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        card_view:cardCornerRadius="@dimen/card_corner_radius"
        card_view:contentPadding="@dimen/material_spacing_small">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical">

            <TextView
                android:id="@+id/bus_route_name"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:text="@{busRoute.name}"/>

            <TextView
                android:id="@+id/bus_route_number"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:text="@{busRoute.numberFormatted}"/>

        </LinearLayout>

    </android.support.v7.widget.CardView>

</layout>